// File: ellipse.h
#ifndef ELLIPSE_H
#define ELLIPSE_H

#include "../point/point.h"

typedef struct {
    Point *p;
    int width;
    int height;
} Ellipse;

// Fonctions pour l'ellipse si nécessaire
// void draw_ellipse(Ellipse *e);
// etc.

#endif